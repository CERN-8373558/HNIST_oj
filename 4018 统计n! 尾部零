#include <stdio.h>

int countZero(int n) {
   int count = 0;
   while (n > 0) {
       count += n / 5;
       n /= 5;
   }
   return count;
}

int main() {
   int n;
   scanf("%d", &n);
   printf("%d\n", countZero(n));
   return 0;
}
