#include <stdio.h>
int main()
{
  int n;float sum,cern;
  sum=0;
  scanf("%d",&n);
  int a[n];
  for(int i=1;i<=n;i++){
  scanf("%d",&a[i]);
  sum =sum+a[i];}
  cern=sum/n;
  printf("%.2f",cern);
  return 0;
}
