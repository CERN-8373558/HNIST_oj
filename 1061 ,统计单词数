#include <stdio.h>
int main()
{
  char a[1000];char word;
  int sum=0;
  int b=0;
  gets(a);
  int i=0;
  for(i=0;(word=a[i])!='\0';i++){
    if(word==' '){
    b=0;}
    else if(b==0){
      b=1;sum++;}}
  printf("%d",sum);
  return 0;
}
