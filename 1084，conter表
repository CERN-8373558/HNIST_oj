#include<stdio.h>
int main()
{
	int n = 0;
	int i = 0;
	scanf("%d", &n);
	for (i = 1; n > i; i++)
		n -= i;
	if (i % 2 == 0)
		printf("%d/%d\n", n, i + 1 - n);
	else
		printf("%d/%d\n", i + 1 - n, n);
	return 0;
}
